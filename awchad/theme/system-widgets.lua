local xrdb = require('awchad.theme.xrdb')

return {
  font = 'JetBrainsMono Nerd Font Medium 12',
  spacing = xrdb.dpi(12),

  kernel = xrdb.color09,
  cpu = xrdb.color03,
  memory = xrdb.color02,
  date = xrdb.color14,
  network = xrdb.color04,
  disk = xrdb.color06,
}
